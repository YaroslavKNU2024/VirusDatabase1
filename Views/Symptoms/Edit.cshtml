@model VirusDatabaseNew.Symptom

@{
    ViewData["Title"] = "Редагувати";
}

<h1>Редагування інформації про симптом @Html.DisplayFor(modelItem => modelItem.SymptomName)</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="SymptomName" class="control-label"></label>
                <input asp-for="SymptomName" class="form-control" />
                <span asp-validation-for="SymptomName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Зберегти" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад до списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
