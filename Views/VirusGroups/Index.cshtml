@model IEnumerable<VirusDatabaseNew.VirusGroup>

@{
    ViewData["Title"] = "групи вірусів";
}

<h1>Групи вірусів:</h1>

<p>
    <a asp-action="Create">Додати групу вірусів</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupInfo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupDateDiscovered)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupInfo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupDateDiscovered)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Редагувати</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Деталі</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
            </td>
        </tr>
}
    </tbody>
</table>
